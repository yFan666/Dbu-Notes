# 线程清洗总结：smarthos-cdms-manager 安岳附页迁移

## 1. 线程核心目的

本线程主要围绕 `smarthos-cdms-manager` 中病案编码页面的附页扩展处理：参考 `smarthos-front` 的提交记录 `f895ce7c`，把安岳县相关病案附页内容迁移到本项目，并兼容此前已经实现的长武附页逻辑。

---

## 2. 用户明确表达过的需求【高可信】

- 【用户原话】修改页面：`C:\XNK_Document\client\smarthos-cdms-manager\src\views\medicalRecord\medicalRecordCoding\codingPage\index.vue`。
- 【用户原话】针对 `C:\XNK_Document\client\smarthos-front\packages\smarthos-med-station\src\views\inpatient-station\pages\medicalRecordsPage\index.vue` 中 `f895ce7c` 这一条提交记录进行处理。
- 【用户原话】新增有关安岳县的附页内容，对于原本内容进行兼容。
- 【用户原话】之前增加了长武附页内容，现在也是根据这一需求进行扩展兼容。
- 【用户原话】`anYueCom/attachedInfo.vue` 中 `followUpFlag` 的 `reqAndDis` 不应写成 `followUpDur,followUpDurUnit`，应仿照页面只写 `reqAndDis: "followUpDur"`。
- 【用户原话】补齐字段类型，并修改 `cpMutatedReason` 的类型。

---

## 3. 用户确认过的结论【高可信】

- 【用户确认】当前项目应优先对齐本项目已有同类页面写法，而不是无条件照搬 `smarthos-front` 的实现。
- 【用户确认】`followUpFlag` 的联动控制应按本项目原附页口径，只控制 `followUpDur`。
- 【用户确认】需要补齐安岳附页新增字段类型，并将 `cpMutatedReason` 类型调整为 `string`。

---

## 4. 可复用经验 / 方法论【中高可信】

- 【用户确认】迁移代码不能只看来源分支，要和当前项目既有同类页面对齐。
- 【AI总结】迁移前应先找当前项目同字段、同组件的写法，差异必须标出来。
- 【AI总结】来源代码中多出来的联动、字段、接口，不应直接照搬；除非当前项目已有同样模式或用户确认。
- 【AI总结】如果差异会影响行为，应先向用户确认，不应自行扩展。

---

## 5. 可继续推进的问题【中可信】

- 【待验证】`anYueCom/attachedInfo.vue` 中 `followUpDur` 仍引用 `followUpDurUnitList`，需要确认该单位列表是否已经保留或需要补回。
- 【待验证】`codingPage/index.vue` 中曾出现调试日志 `console.log("==EmrINfo - data==", data.mrBasicInfo.followUpDurUnit);`，需要确认最终代码中是否已删除。
- 【待验证】安岳附页相关字段类型已补齐，但还需要确认后端实际返回字段类型是否与前端类型一致。
- 【待验证】`smarthos-front` 的 `f895ce7c` 还包含 `recordType.d.ts` 类型变更，当前项目已按安岳附页字段进行补齐，但是否还有非安岳业务字段需要同步，需结合实际页面使用确认。

---

## 6. 幻觉 / 脑补 / 应丢弃内容【低可信】

- 【丢弃】内容：将 `reqAndDis: "followUpDur,followUpDurUnit"` 直接视为本项目可用逻辑。
  原因：这是来源项目写法，本项目原附页只控制 `followUpDur`，用户已指出应按本项目页面处理。

- 【丢弃】内容：未对比本项目原附页写法就直接迁移来源组件配置。
  原因：用户明确要求避免“擅自扩展”，此类迁移方式容易引入不符合当前项目口径的行为。

- 【丢弃】内容：把来源项目中所有改动都默认视为本项目必须同步。
  原因：`f895ce7c` 中还包含药品管理相关无关页面变更，本线程目标只涉及病案编码页面及安岳附页兼容。

---

## 7. 最终压缩版总结

本线程的有效内容是：在 `smarthos-cdms-manager` 的病案编码页面中，参考 `smarthos-front` 提交 `f895ce7c` 迁移安岳县病案附页，并兼容原附页与长武附页。迁移时不能只照搬来源分支，需要优先对齐本项目已有同类页面。已明确 `followUpFlag` 的 `reqAndDis` 应只写 `followUpDur`，不要扩展到 `followUpDurUnit`。同时需要补齐安岳附页涉及的字段类型，并将 `cpMutatedReason` 类型改为 `string`。后续重点是继续检查迁移差异，尤其是单位列表、调试日志、后端字段类型和实际页面行为。
