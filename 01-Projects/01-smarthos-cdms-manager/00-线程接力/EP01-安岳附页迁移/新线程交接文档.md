---
type: project
status: doing
area: 项目
review: weekly
created: 2026-06-23
updated: 2026-06-23
---

# 新线程交接文档：smarthos-cdms-manager 安岳附页迁移

## 1. 当前任务背景

- 这个线程主要围绕 `smarthos-cdms-manager` 病案编码页面的安岳县附页迁移。
- 当前已经处理到：从旧线程中清洗出用户原话、已确认迁移规则、待验证问题和应丢弃做法。
- 新线程需要继续解决：基于已确认内容检查迁移结果，不重新扩大到无关页面或来源提交的全部改动。

---

## 2. 已确认事实【高可信】

- 【用户原话】修改页面是 `smarthos-cdms-manager` 中病案编码页面的 `codingPage/index.vue`。
- 【用户原话】参考来源是 `smarthos-front` 中 `f895ce7c` 这一条提交记录。
- 【用户原话】新增安岳县附页内容，并兼容原本内容。
- 【用户原话】此前已有长武附页内容，本次是在同类需求上扩展兼容。
- 【用户确认】迁移时优先对齐当前项目已有同类页面写法，不无条件照搬 `smarthos-front`。
- 【用户确认】`followUpFlag` 的联动控制只控制 `followUpDur`。
- 【用户确认】需要补齐安岳附页新增字段类型，并将 `cpMutatedReason` 类型调整为 `string`。

---

## 3. 已确定规则【必须遵守】

- 迁移代码不能只看来源分支，要对齐当前项目既有同类页面。
- 来源项目中多出来的联动、字段、接口，不直接照搬。
- 差异会影响行为时，先向用户确认，不自行扩展。
- 本线程只处理病案编码页面及安岳附页兼容，不扩展到来源提交中的无关页面。

---

## 4. 当前有效结论【可继续使用】

- `followUpFlag` 的 `reqAndDis` 应使用 `followUpDur`，不使用 `followUpDur,followUpDurUnit`。
- 安岳附页相关字段类型需要补齐。
- `cpMutatedReason` 类型应调整为 `string`。
- 后续检查重点是迁移差异、单位列表、调试日志、后端字段类型和实际页面行为。

---

## 5. 待验证内容【不能直接当事实】

- `anYueCom/attachedInfo.vue` 中 `followUpDur` 是否仍需要 `followUpDurUnitList`。
- `codingPage/index.vue` 中调试日志是否已删除。
- 后端实际返回字段类型是否与前端类型一致。
- `f895ce7c` 中是否还有非安岳业务字段需要同步。

---

## 6. 不可信 / 应丢弃内容【禁止沿用】

- 直接使用 `reqAndDis: "followUpDur,followUpDurUnit"`。
  原因：这是来源项目写法，用户已确认当前项目只控制 `followUpDur`。

- 未对比当前项目原附页写法就迁移来源组件配置。
  原因：容易引入不符合当前项目口径的行为。

- 默认同步 `f895ce7c` 的全部改动。
  原因：来源提交包含无关页面变更，本线程目标只涉及病案编码页面和安岳附页兼容。

---

## 7. 下一步任务

- 第一优先级：检查当前代码是否符合 `followUpFlag`、字段类型和 `cpMutatedReason` 的已确认结论。
- 第二优先级：核对待验证项，尤其是单位列表、调试日志和后端字段类型。
- 暂不处理：来源提交里的无关页面和未被用户确认的额外字段同步。

---

## 8. 对新线程的要求

- 不要重新发散到整个来源提交。
- 不要把待验证内容当作已完成事实。
- 不要自行扩展联动字段或接口字段。
- 优先基于当前项目已有写法和用户确认结论继续推进。
